package projetcontrollers.api

import play.api.mvc._
import javax.inject._
import play.api._


/**
 * This controller creates an `Action` to handle HTTP requests to the
 * application's home page.
 */
class projet (

)

